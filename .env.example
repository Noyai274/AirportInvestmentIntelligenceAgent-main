# Copy to .env and fill in. .env is gitignored.
ANTHROPIC_API_KEY=sk-ant-...        # required — powers the agent (agent/agent.py)
SODA_APP_TOKEN=                     # optional — Socrata app token, avoids throttling on the first BTS pull (ingest/soda.py)
